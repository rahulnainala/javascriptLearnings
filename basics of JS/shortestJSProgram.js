//JS Engine will still work
//check in the Console use => window or this

/**
 * A window file and this keyword is created along
 * with global execution context.
 *
 * All the Browsers have a responsbility to create
 * these global objects.
 *
 * this === window (true)
 */
